.header


.search_box
  = form_with url: search_books_path, method: :get, local: true do |f|
    .search_box__form-group
      = f.text_field :keyword, class: "form-control"
      = f.submit '本の題名を検索', class: "form-control btn btn-success"
- if @books.present?
  = render 'books/book'

.footer
